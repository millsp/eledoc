<ul class="tsd-type-parameters">
    {{#each typeParameters}}
        <li>
            {{#compact}}
                <span class="name">{{name}}</span>
                {{#if type}}
                    <span class="tsd-signature-symbol">:&nbsp;</span>
                    {{#with type}}{{> type}}{{/with}}
                {{/if}}
            {{/compact}}
            {{> comment}}
        </li>
    {{/each}}
</ul>